import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#4d83fb",
    },
    secondary: {
      main: "#4d83fb",
    },
    lightBoxHeaderIcon: {
      main: "#ffffff",
    },
  },
});
